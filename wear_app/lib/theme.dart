import 'package:flutter/material.dart';

/// Colores de la app del reloj.
///
/// Fondo negro puro: en la pantalla OLED de un reloj cada píxel negro está
/// apagado y no gasta batería.
const Color wearBackground = Color(0xFF000000);
const Color wearSurface = Color(0xFF14181F);
const Color wearMuted = Color(0xFF9DADC8);
const Color wearPrimary = Color(0xFF7CC4FF);
